node_modules
npm-debug.log
.git
.gitignore
*.log

# Local runtime data — must not be baked into the image; these are created
# fresh by src/config.js (fs.mkdirSync) when the container starts.
data
output
cache

# Windows-only local helper, irrelevant inside the container
start.bat
